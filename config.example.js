// Copy this file to config.local.js for local preview, or generate config.local.js in CI.
// These are public browser values. Never put service-role keys or VAPID private keys here.
window.AQUATRACKER_ENV = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  VAPID_PUBLIC_KEY: ''
};
